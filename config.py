# Training configuration
CONFIG = {
    "latent_dim": 100,
    "img_channels": 3,
    "img_size": (64, 64),
    "batch_size": 32,
    "learning_rate": 0.0002,
    "num_epochs": 100,
    "save_interval": 10,
    "output_dir": "output",
}
